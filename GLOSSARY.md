## 译名约定

正文默认使用中文术语；保留稳定英文专名与代码标识符。

| 推荐写法 | 避免 / 说明 |
|------|------|
| 智能体 | 泛指时少用裸写 Agent；专名保留如 Fork Agent、Explore Agent |
| 子智能体 | 少用 Sub-Agent / sub-agent（URL、原文标题除外） |
| Hook | 可括注「钩子」；技术讨论优先 Hook |
| 粘性锁存器 | 少用未加中文的 Sticky Latch |
| 查询循环 | 图示缩写可保留 Query Loop / QL |
| 提示词缓存 | Prompt Cache 可并存于首次出现 |

## 智能体（Agent）

由大语言模型驱动、可自主选择并调用工具以完成任务的运行时实体。本书统一用「智能体」指代 agent；保留英文专名时写作 Agent（如 Fork Agent）。

## 查询循环（Query Loop）

智能体的核心控制流：流式接收模型输出、执行工具、评估结果并决定是否继续。Claude Code 中以异步生成器实现。

## Bootstrap State

进程级可变单例，保存会话 ID、模型配置、成本计数、路径缓存等基础设施状态。启动早期即可用，不驱动 UI 重渲染。

## AppState

面向界面的响应式状态仓库（Zustand 风格），保存消息、输入模式、工具审批、进度指示等会频繁变化的 UI 状态。

## 粘性锁存器（Sticky Latch）

一旦在会话中启用某项配置（如 beta header、缓存相关开关），就不再在会话中途关闭，以避免破坏提示词缓存稳定性。

## 提示词缓存（Prompt Cache）

云端对系统提示词与消息前缀的字节级缓存。前缀不稳定会导致大量 Token 成本浪费；本书多处设计都服务于缓存命中。

## Hook

生命周期钩子。在工具调用前/后、停止等时机介入，可拦截、改写或注入行为。常见如 `PreToolUse`、`PostToolUse`、`Stop`。正文优先写 Hook，必要时括注「钩子」。

## Swarm

多智能体协作形态：多个智能体通过任务状态机与消息通道协同完成工作，而不是单次线性委派。

## Fork Agent

通过共享字节级相同的提示词前缀来复用提示词缓存的子智能体策略，可显著降低并行任务的输入 Token 成本。

## 子智能体（Sub-Agent）

由主智能体通过 `AgentTool` 等机制派生的独立查询循环，拥有自己的消息历史、工具集与权限模式。

## MCP

Model Context Protocol，通用工具协议。用于把外部工具与资源以统一传输与鉴权方式接入智能体。

## Skills

可扩展技能包：启动时只加载元数据（frontmatter），真正调用时再加载完整内容，以控制启动成本与上下文体积。

## 信任边界（Trust Boundary）

启动流水线中「是否信任当前工作区」的分界。信任前后允许执行的初始化与 I/O 范围不同。

## Print 模式

非交互式运行模式（如 `--print`）：不进入完整 REPL，但仍汇入同一套查询循环。
